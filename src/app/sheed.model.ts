export class Sheed {
    
    constructor(sheed: string, quanty: number, width:string, height: string) {
        this.sheed = sheed;
        this.quanty = quanty;
        this.width = width;
        this.height = height;
    }

    sheed: string;
    quanty: number;
    width:string
    height:string
}